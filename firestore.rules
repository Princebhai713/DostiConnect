rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all reads and writes by default.
    // The application has been simplified and has no database access requirements.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
